syntax = "proto3";

package protos;

option go_package = "proto/buses";

service BusService{
  rpc RequestBus(BusRequest) returns (stream BusResponse) {}

}
message BusRequest {
  int64 BusNumber = 1;
}

message BusResponse {
  double longitude = 1;
  double latitude = 2;
}